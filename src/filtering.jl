function filter_bp_fmri(fmri, low, high):
    # fmri: 4D time series
    # low: lower cutoff frequency for bandpass filter
    # high: upper cutoff frequency for bandpass filter

    # returns save the NIFTI locally appending a _bp
    # inherits scipy.signal butter filtfilt (Butterworth)
end
